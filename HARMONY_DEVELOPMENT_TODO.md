# EggClip HarmonyOS 开发 TODO

本清单只记录可验收功能、真机验收和明确待办。实现细节、测试过程与排障记录写入 handoff 或提交说明；后续开发只勾选既有项目，不新增子项。

## 当前阶段

- HarmonyOS 端已具备前台 POC、邀请配对、HUKS 空间密钥、同一认证会话内的正式 `ITEM_LIVE` 收发和可信设备展示。
- 当前主线：已配对设备的正式连接生命周期、断线补同步和双端真机验收。
- 目标：SDK `6.1.1(24)`，phone/tablet；只在前台发现和连接。

## H0：工程与安全基线

- [x] DevEco Stage Model 工程、目标目录、主题、图标、底部导航和设置入口。
- [x] 签名配置脱敏；仓库不保存证书、密码或本机路径。
- [ ] 统一 ArkTS 格式、lint 与测试脚本。

## H1：前台网络与剪贴板 POC

- [x] 前台 mDNS、严格 IPv4 手动 WebSocket、网络诊断和生命周期基础清理。
- [x] 真实 PasteButton 读取、用户触发复制、文本/帧大小边界和 POC 收发。
- [x] 真机 POC 回归与 VPN/TUN、AP 隔离、防火墙排障提示。

## H2：本地数据与页面

- [x] 领域模型、RDB migration、历史、设置、主题和 retention。
- [x] 首页、配对页、设备页、设置页及手机端悬浮底部页签。
- [x] 首页历史、发送状态、可信设备列表与 POC 运行时设备边界展示。
- [ ] 平板双栏布局、历史正文预览/复制/详情展开。

## H3：设备身份与本地密钥

- [x] 本机 deviceId、Ed25519/X25519/HKDF/AES-GCM 服务边界与共享向量结构。
- [x] HUKS 长期身份、空间密钥 AES-GCM 本地加密和 HMAC-SHA-256 摘要别名。
- [x] 设置页空间密钥加解密与摘要自检入口。
- [ ] HarmonyOS 6.1 真机确认 Ed25519、X25519、HMAC 与桌面端向量/互通结果。
- [ ] 密钥缺失、损坏和重新初始化流程。

## H4：邀请与配对

- [x] 扫码、PasteButton/输入导入、邀请校验、确认码与邀请秘密清理。
- [x] CLIENT_HELLO、SERVER_HELLO、AUTH_PROOF、AUTH_OK、空间密钥交付和 trusted device 持久化。
- [x] 真机配对与 HUKS AES 空间密钥导入已验证。
- [ ] 过期、重复、身份/空间不匹配邀请的双端真机回归。

## H5：认证连接与同步协议

- [x] 加密 envelope、会话状态门控、AEAD、replay guard、认证后 `ITEM_LIVE` 收发。
- [x] PasteButton 正式出站：本地事务、HUKS HMAC 摘要、本地正文加密和认证发送；未认证时保留 POC 回退。
- [x] `SYNC_HEADS` 协议载荷校验基础。
- [ ] 已配对设备的正式连接生命周期、前台自动连接、单连接去重、心跳和重连。
- [ ] `SYNC_HEADS` 实际发送、范围请求、`ITEM_BATCH`、ACK、retention gap 与断线续传。
- [ ] 网络失败的待同步记录与可靠发送状态。

## H6：设备、设置与移动体验

- [x] 发送/接收/失败/暂停状态、正式认证会话状态、可信设备与 POC 边界、主题和隐私诊断。
- [ ] 设备重命名、移除、密钥轮换和真实在线状态。
- [ ] 完整首次使用、网络失败、平板适配、前后台/网络切换恢复体验。

## H7：测试与发布准备

- [ ] 覆盖 sync、ConnectionManager、stores、UI smoke 与跨端协议互通的自动化测试。
- [ ] 手机/平板真机回归：网络切换、前后台、锁屏、配对异常、PasteButton、Emoji/最大文本/连续发送。
- [ ] 正式签名、隐私/权限说明、发布包秘密检查、升级与回滚清单。

## 暂不计划

- 后台常驻、后台静默剪贴板读取、桌面卡片和后台任务。
- 图片、文件、HTML、富文本、账号、云服务、公网中继、遥测和自动更新。
- 多同步空间、团队权限和依赖 `READ_PASTEBOARD` system_basic 的发布方案。
